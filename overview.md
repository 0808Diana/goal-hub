# 成长工作台 · index.html 总览（V2 个人成长系统）

单文件离线工作台 `index.html`（原 `goal-hub.html`，约 142 KB，0 外部框架/CDN/字体依赖、0 emoji，断网可用，全部 CSS/JS/SVG 内联）。当前版本在「学习目标管理 + K12 AI 情报」地基之上，叠加了 **V2 个人成长系统**：成长地图（方向 → 节点 → 任务）+ 时间节奏（时间槽 + 工作状态）。

> 🔄 **2026-08-24 已迁移到 GitHub Pages（固定链接，不再漂移）**：部署链接 **https://0808Diana.github.io/goal-hub/** 。源文件由 `goal-hub.html` 改名 `index.html`（Pages 入口）；情报清单外部化为 `intel.json`（运行时拉取，每月 1 号自动化重搜更新，改此文件无需重新部署）。改代码后 `git push` 即上线，链接永不变。下方历史记录中的旧 CloudStudio 链接（`8209f709…`、`66a4a00f…` 等）均已失效，仅作存档。

---

## 五大 Tab（移动端底部 4 固定 Tab + 顶部品牌，PC 左侧栏）

1. **今日** — 顶部「今日工作状态」（轻松/正常/忙碌，温柔不制造负罪感）+「一天的节奏」（4 个时间槽排布今日任务）；逾期/今天该做/快到期的置顶，红色标逾期，一键完成。
2. **成长地图** — 方向中心。4 个固定顶层方向（健康/工作能力/认知/生活），方向下自建节点，节点承载目标、关联任务、聚合打卡记录。
3. **看板** — 目标卡片（进度/连续天数/预测/建议）、休息日逻辑、最近记录（含打卡时刻 + 投入分钟）、柱状图（14 天投入分钟）。
4. **周报** — 4 列复盘（保持/问题/尝试/下周预案）+ 本周记录明细，一键生成格式化文本。
5. **我** — 数据导出/导入 JSON、CSV、清空（二次确认）、设置。

---

## V2 成长地图（本次改造核心，不推翻原有任务/打卡）

- **4 个固定方向**（`DIRECTIONS`）：🧘健康 / 💼工作能力 / 🌏认知 / 🌿生活（图标全部内联 SVG，无 emoji）。系统**不预置**具体内容，用户自建节点。
- **节点（node）**：字段 = 名称、所属方向、描述、状态（未开始/进行中/暂停/已完成）、当前目标、关联任务、创建/更新时间。支持增删改 + 状态切换。
- **节点 ↔ 任务打通**：任务表单新增「所属成长方向」「所属成长节点」；节点详情自动聚合关联任务及其打卡记录（**不重复存打卡数据**，记录经关联任务自动归属节点）。
- **示例节点**：首次打开预置 1 个「示例节点（可删）」（工作能力/进行中），让地图不为空，可一键清空。

## V2 时间节奏

- **4 个时间槽**（`TIME_SLOTS`）：晨间成长 07:30–08:20 / 工作 09:30–18:30 / 晚间生活 21:00–22:00 / 收尾 22:00–23:00。
- **任务字段新增**：预计时长（分钟）、适合时间（晨间/工作空档/晚间/周末，多选）。
- **今日工作状态**：轻松 / 正常 / 忙碌。忙碌 = 少安排成长任务、不推送负罪感（晨间槽降级为「少安排」）。
- **今日页重组**：围绕「一天的节奏」而非纯待办列表排布。

## 视觉

日系清新 × 生活手账 × 自然：米白/奶油底、鼠尾草绿主色、淡蓝/米黄/杏粉点缀，大留白、圆角、柔和阴影；规避高饱和、游戏化、KPI 感。

---

## 情报模块（既有，保留）

- 内嵌 `INTEL_ITEMS`：18 篇真实资讯（政策/产品/资本/国际/深度），带**真实链接**、来源、日期、**热度 0–100** 与「为什么该读」。
- 排序按热度 / 原顺序；近 30 天筛选；今日卡点标题开原文、圆圈记已读。
- 每月 1 号自动化 WebSearch 重搜上月资讯 → 重写清单 → 重部署；`syncIntelItems()` 按标题保留已读状态。

## 数据模型

- 本地 `localStorage`，key `wb_goalhub_data`（schema v3）。
- `normalize()` 做老数据迁移：目标含 `dirId/nodeId/estMin/fitTimes`；`nodes` 数组、`statusByDate`、`mapView` 均有兜底。
- 数据积累到 30 条温和提示导出；首屏含导出/导入；导入不限条数；清空二次确认。

---

## 验收链路（已满足）

地图定方向 → 节点承载目标 → 任务负责执行（标注时长/适合时间）→ 时间负责节奏（4 槽排布）→ 打卡负责记录（归属节点）→ 周报负责调整。

## 校验

- `node --check` 内联脚本通过；0 外部框架/CDN、0 emoji。
- DOM-stub 冒烟测试通过：DIRECTIONS=4 / TIME_SLOTS=4 / seed 节点 1 条（work/进行中）；`goalNodeTag(空)=""`，关联后 `relatedTasksOf=1` 且标签含节点名；`computeGoal gA` 进度 12.8% 不受影响；`normalize` 能往返 nodes/dirId/estMin/fitTimes。
- 已重新部署，同一链接指向最新文件。

---

## V2.1 成长地图视觉升级（综合星图）

应需求「地图更美观、围绕四方向、可建子节点、悬停看子节点、点击下钻」，在 V2 地图之上做了一次纯视觉 + 交互增强（数据模型与任务/打卡关联逻辑不变）。

- **综合星图（renderMapComposite）**：中心「我的成长」圆点向四角放射 4 个方向 hub（健康/工作能力/认知/生活，各自配色 + SVG 图标）；方向下的顶层节点沿放射方向弧形排开；每个顶层节点再向外挂它的子节点。节点位置用向量运算自动算（方向单位向量 → 旋转角度 → 半径），连线是内联 SVG `<line>`（按方向着色、半透明），整张图随容器自适应（viewBox + aspect-ratio）。
- **子节点（父子层级）**：节点数据新增 `parent` 字段。`childrenOf / topNodesOf / descendantIds` 辅助函数；节点表单新增「上级节点」下拉（自动排除自身及其后代，避免成环）；删除节点时把子节点上挂到其上级（不丢节点）；节点详情新增「子节点」区，可一键「添加子节点」。
- **悬停看更细**：鼠标悬停方向 hub 或节点 chip，弹出 `.map-tip` 浮层——方向显示其下节点与子节点数；节点显示状态、关联任务数、子节点清单。悬停不挡操作，移开即隐。
- **点击下钻 / 沿途创建**：点中心回首页；点方向 hub 进方向列表；点节点 chip 进节点详情；方向 hub 与节点 chip 各带一个「+」按钮，分别建节点 / 建子节点，无需先进入列表。
- **视觉风格**：日系清新 × 生活手账 × 自然——奶油底、圆角（hub 圆形 + 节点药丸形）、柔和阴影、低饱和；chip 用状态色圆点、子节点数角标；移动端做了尺寸收敛（≤480px 缩小 hub/chip）。
- **校验**：`node --check` 通过；0 外部框架/CDN、0 emoji；DOM-stub 冒烟测试 17 项全过（含 renderMapComposite 输出 map-canvas/mapchip/maphub/add-subnode、子节点层级 childrenOf/descendantIds、节点详情含 add-subnode、normalize 往返 parent）。
- 已重新部署，同一链接指向最新文件。

---

## V2.2 成长地图改版：十字路口手绘风 + 聚焦下钻 + 节点多任务关联

应新需求重做地图交互与视觉：**子节点只命名 + 归属上级节点（可无限递归）；每个节点可关联看板里的 1..N 个任务；UI 参考日本简笔画地图，从十字路口展开，卡通小人站在「当前所在节点」，点节点展开它的子节点**。

- **聚焦下钻（focus 模型）**：`S.settings.mapView.focus` ∈ `center` / `dir:ID` / `node:ID`。中心是「我的成长」十字路口，四个方向（健康/工作能力/认知/生活）从路口放射；点方向进入该方向，点节点展开它的子节点，卡通小人始终站在当前 focus 节点上。无限递归：子节点下还能建子节点，点击即下钻。顶部面包屑（我的成长 › 方向 › 父节点 › 当前节点）可一键回跳；还有「返回上级」浮标。
- **卡通人物 + 手绘道路**：内联 SVG 小人（圆头、笑脸、腮红、四肢）站在当前节点；节点间用 SVG `<path>` 手绘虚线道路（带轻微弯曲控制点，dasharray 模拟手绘感）；画布是米白纸纹 + 点阵网格（手账感）。方向节点用彩色圆 hub + 名称，节点用状态色圆点药丸。
- **子节点只需命名 + 归属上级**：节点表单只填「名称」+「上级节点」（选填，默认该方向顶层）；子节点自动归属上级 `parent`。删节点时子节点上挂到上级，不丢。
- **节点 ↔ 任务 1..N 关联**：数据模型新增 `node.taskIds:[]`（唯一关联存储）；看板任务可在「节点详情」里「关联看板任务」多选勾选（openLinkTaskSheet + save-link-task），也可在节点里「取消关联」（node-unlink-task）。看板建任务时选「所属成长节点」会同步写入 taskIds（linkTaskToNode）。旧 `goal.nodeId` 经 migrateTasks() 一次性迁到 taskIds。`goalNodeTag` 显示首个节点名 +「+N」多关联。节点详情的任务列表、打卡、记录均按 taskIds 聚合。
- **校验**：node --check 通过；0 外部框架/CDN、0 emoji；DOM-stub 冒烟测试 27 项全过（递归 childrenOf/descendantIds、taskIds 关联与多关联、聚焦图含 focusmk/char/mapnode/mapparent、中心含 crossroads、focusPath 链路、openLinkTaskSheet、完整 renderMap 含面包屑、normalize 往返 parent/taskIds）。
- 已重新部署，同一链接指向最新文件。

---

## V2.3 六处修正（本次）

按截图与需求对地图、节点、关联做了 6 项修正，数据模型与既有任务/打卡逻辑不变。

1. **今日页时间槽「黑色徽章」改柔和色**：原时间槽左侧深色/黑底时间徽章改为日系清新低饱和主题色（`--muted` / 方向柔和色），去掉黑底，与整体奶油手账风一致。
2. **看板页去掉「新建目标」按钮**：目标统一在「今日」或节点详情关联/新建，看板只做查看与复盘，不再提供新建入口（顶栏仅留标题与空态提示）。
3. **节点父子固定为只读信息展示**：从方向（如健康）建节点 A，健康即 A 的父，固定不可改；A 上建子节点 1，A 即 1 的父，固定不可改。新建/编辑节点时，方向与上一级节点以**只读文本 pill** 展示（`openNodeModal` 不再用下拉填写，保存时固定用传入的 `dirId`/`parent`），避免误选当前节点为父导致环。
4. **目标 ↔ 节点 N 对 N 双向联动**：关联模型从「单节点」升级为「多对多」。每侧独立可多选——目标表单「关联成长节点」改为**多选复选框组**（`nodeCheckboxes`）；节点详情「添加目标」按钮列出全部已建目标可**多选/单选勾选**（`openLinkTaskSheet`）。底层用 `setGoalNodes` / `setNodeTasks` 双向同步 `goal.nodeIds[]` 与 `node.taskIds[]`，任一侧增删都会反向清理另一侧旧关联；`delGoal`/`delNode` 同步清理对方。
5. **节点「添加目标」面板**：`renderNodeDetail` 的「关联任务」区改为「添加目标」按钮（`node-link-task`），弹出面板列出所有目标（checkbox 显示已勾选），保存写回 `node.taskIds` 并反向写 `goal.nodeIds`；每条已关联目标可「取消关联」（`node-unlink-task`，双向移除）。旧 `goal.nodeId` 经 `migrateTasks()` 迁到 `nodeIds[]`，不丢历史。
6. **地图 UI 缩小 + 手绘风**：参考 kitkit-agent.com 的简笔画风，地图整体收敛——`.map-canvas` 加 `max-width:540px` 并居中、节点 hub/药丸/chip 尺寸下调、放射半径 R 与边界 padding 收紧，使图更紧凑；连线启用已有的 `#wobble` 手绘抖动滤镜（`filter:url(#wobble)` 作用于 `.map-svg`），虚线道路带轻微手绘波动；装饰小猫 `.mapcat` 固定到右下角，不再游离。

- **校验**：`node --check` 内联脚本通过；0 外部框架/CDN、0 emoji。DOM-stub 冒烟测试通过：seed 4 目标 / 1 节点；`renderMapComposite` 输出 map-canvas（含 wobble 滤镜）正常；`goalForm(null)` 输出含 `f-nodes` 多选复选框；`renderNodeDetail(节点)` 正常；N 对 N `setGoalNodes` 双向写入 `node.taskIds` + `goal.nodeIds` 均命中；`openLinkTaskSheet` 正常弹出。
- **已重新部署（链接已变更）**：新链接 **https://8209f709ea184362beb73dcf53a8fd82.gz4.agentos-app.net**；旧 `66a4a00fe0f7412b9561e68146182095.sh5…` 已失效，Notion `/embed` 需更新。

---

## V2.4 四处交互修正（本次）

1. **补卡也能填时长**：情报弹窗的「补记日期」旁新增「本次约 __ 分钟」输入（写入 `S.settings.punch[gid].minutes`，打勾时一并计入 minutes，进 14 天分钟柱状图）。普通打卡卡片的补记本就带分钟输入，本次补齐弹窗路径。Enter 快捷键改为仅「完成量」输入框触发打卡，避免在分钟框回车误打卡。
2. **节点可创建也可删除**：节点详情页底部新增「删除这个节点」（红色、二次确认），复用 delNode——子节点升级上一级、关联目标不删只是解除挂载、删除后视图回退父级。
3. **节点悬浮不再展示信息，点击才展示**：移除地图上所有 hover 提示（MAP_TIPS / showMapTip / bindMapTip / nodeTip 及 data-tip 全部删除）。节点 chip 点击 = 打开节点详情（展示信息）；chip 上「⇓」= 展开子节点（原点击行为），「+」= 加子节点；方向仍是点击进入。
4. **关联目标面板 = 表格式多选 + 双向可见**：「添加目标」面板整行可点（label 原生切换），隐藏原生 checkbox 改用自定义 `.lkbox` 对勾，选中行 `.on` 高亮（change 监听即时同步 class）；`goalNodeTag` 升级展示前 2 个节点名 +「+N」+ title 全量悬浮，并补到看板卡 gmeta（今日卡本就有）——目标与节点的关联两侧都可见。

- **校验**：`node --check` 通过；0 外部框架/CDN、0 emoji；smoke.js 冒烟测试通过（renderMapComposite / goalForm / renderNodeDetail 含新删除区 / N对N / 关联面板均正常）。
- **已重新部署**：同一链接 **https://8209f709ea184362beb73dcf53a8fd82.gz4.agentos-app.net** 指向最新文件（Notion `/embed` 无需改）。

